import React from "react";
import { ClerkAuthWrapper } from "../../components/auth/ClerkAuthWrapper";

const SignUpPage: React.FC = () => {
  return <ClerkAuthWrapper mode="sign-up" />;
};

export default SignUpPage;
